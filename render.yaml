services:
  - type: web
    name: ecome-finance
    runtime: python
    buildCommand: bash build.sh
    startCommand: gunicorn ecome_finance.wsgi --log-file -
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
